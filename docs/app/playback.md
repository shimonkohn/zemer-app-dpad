# Playback and player documentation

## Playback-related tracked Kotlin files

| File | Lines | Package | Declarations |
| --- | ---: | --- | --- |
| `app/src/main/kotlin/com/jtech/zemer/constants/MediaSessionConstants.kt` | 19 | `com.jtech.zemer.constants` | object MediaSessionConstants, val CommandToggleLibrary, val CommandToggleLike, val CommandToggleStartRadio, val CommandToggleShuffle, val CommandToggleRepeatMode |
| `app/src/main/kotlin/com/jtech/zemer/playback/AudioOnlyRenderersFactory.kt` | 25 | `com.jtech.zemer.playback` | class AudioOnlyRenderersFactory |
| `app/src/main/kotlin/com/jtech/zemer/playback/DownloadUtil.kt` | 361 | `com.jtech.zemer.playback` | class DownloadUtil, val databaseLazy, val databaseProvider, val mediaStoreDownloadManager, val database, val connectivityManager, val audioQualityFlow, var audioQuality, val sharedUrlCache, fun invalidateUrl, val songUrlCache, val scope, val downloads, val exoDownloadStates, val dataSourceFactory, val mediaId, val length, val currentQuality, val playbackData, val format, val contentLength, val now, val existing, val updatedSong, val baseStreamUrl |
| `app/src/main/kotlin/com/jtech/zemer/playback/ExoDownloadService.kt` | 111 | `com.jtech.zemer.playback` | class ExoDownloadService, class TerminalStateNotificationHelper, val context, val notificationHelper, var nextNotificationId, val notification |
| `app/src/main/kotlin/com/jtech/zemer/playback/MediaLibrarySessionCallback.kt` | 655 | `com.jtech.zemer.playback` | class MediaLibrarySessionCallback, val databaseLazy, val downloadUtil, val database, val scope, var toggleLike, var toggleStartRadio, var toggleLibrary, val connectionResult, val whitelistedArtistIds, val whitelistedArtistIds, val whitelistedArtistIds, val likedSongCount, val downloadedSongCount, val downloads, val searchResults, val localSongs, val artistSongs, val albumSongs, val playlistSongs, val allLocalSongs, val searchItems, val whitelistedItems, val onlineResults, val defaultResult |
| `app/src/main/kotlin/com/jtech/zemer/playback/MediaStoreDownloadManager.kt` | 728 | `com.jtech.zemer.playback` | class MediaStoreDownloadManager, val databaseLazy, val database, val scope, val mediaStoreHelper, val connectivityManager, val audioQuality, val httpClient, val downloadSemaphore, val _downloadStates, val downloadStates, val downloadQueue, val activeDownloads, class DownloadState, val songId, val status, val progress, val bytesDownloaded, val totalBytes, val error, val retryAttempt, class Status, fun downloadVideo, val currentState, val videoSong |
| `app/src/main/kotlin/com/jtech/zemer/playback/MediaStoreDownloadService.kt` | 307 | `com.jtech.zemer.playback` | class MediaStoreDownloadService, val scope, var hasStartedForeground, var lastCompletedCount, fun start, val intent, fun stop, fun ensureForegroundService, val notification, val songId, fun createNotificationChannel, val channel, fun createInitialNotification, fun updateNotification, val activeDownloads, val completedCount, val notification, fun createSingleDownloadNotification, val song, val title, val artist, val progress, val progressLabel, val downloaded, val total |
| `app/src/main/kotlin/com/jtech/zemer/playback/MusicService.kt` | 1590 | `com.jtech.zemer.playback` | class MusicService, val database, var audioFocusRequest, var lastAudioFocusState, var wasPlayingBeforeAudioFocusLoss, var hasAudioFocus, val scope, val binder, val waitingForNetworkConnection, val isNetworkConnected, val audioQualityFlow, var audioQuality, var currentQueue, var queueTitle, val currentMediaMetadata, val currentSong, val currentFormat, val playerVolume, var isAudioEffectSessionOpened, var loudnessEnhancer, var lastPlaybackSpeed, val automixItems, var consecutivePlaybackErr, val songUrlCache, val sessionToken |
| `app/src/main/kotlin/com/jtech/zemer/playback/PlayerConnection.kt` | 207 | `com.jtech.zemer.playback` | class PlayerConnection, val database, val service, val player, val playbackState, val playWhenReady, val isPlaying, val mediaMetadata, val currentSong, val currentLyrics, val currentFormat, val queueTitle, val queueWindows, val currentMediaItemIndex, val currentWindowIndex, val shuffleModeEnabled, val repeatMode, val canSkipPrevious, val canSkipNext, val error, val waitingForNetworkConnection, fun playQueue, fun startRadioSeamlessly, fun playNext, fun playNext |
| `app/src/main/kotlin/com/jtech/zemer/playback/SleepTimer.kt` | 68 | `com.jtech.zemer.playback` | class SleepTimer, val scope, val player, var sleepTimerJob, var triggerTime, var pauseWhenSongEnd, val isActive, fun start, fun clear |
| `app/src/main/kotlin/com/jtech/zemer/playback/queues/EmptyQueue.kt` | 14 | `com.jtech.zemer.playback.queues` | object EmptyQueue |
| `app/src/main/kotlin/com/jtech/zemer/playback/queues/ListQueue.kt` | 19 | `com.jtech.zemer.playback.queues` | class ListQueue, val title, val items, val startIndex, val position |
| `app/src/main/kotlin/com/jtech/zemer/playback/queues/LocalAlbumRadio.kt` | 65 | `com.jtech.zemer.playback.queues` | class LocalAlbumRadio, val albumWithSongs, val startIndex, val database, val endpoint, var continuation, var firstTimeLoaded, val nextResult, val filteredItems, val nextResult, val filteredItems |
| `app/src/main/kotlin/com/jtech/zemer/playback/queues/Queue.kt` | 40 | `com.jtech.zemer.playback.queues` | interface Queue, val preloadItem, fun getInitialStatus, fun hasNextPage, fun nextPage, class Status, val title, val items, val mediaItemIndex, val position, fun filterExplicit, fun List |
| `app/src/main/kotlin/com/jtech/zemer/playback/queues/YouTubeAlbumRadio.kt` | 60 | `com.jtech.zemer.playback.queues` | class YouTubeAlbumRadio, var playlistId, val database, val endpoint, var albumSongCount, var continuation, var firstTimeLoaded, val albumSongs, val filteredSongs, val nextResult, val filteredItems |
| `app/src/main/kotlin/com/jtech/zemer/playback/queues/YouTubeQueue.kt` | 58 | `com.jtech.zemer.playback.queues` | class YouTubeQueue, var endpoint, val database, var continuation, val nextResult, val filteredItems, val nextResult, val filteredItems, fun radio |
| `app/src/main/kotlin/com/jtech/zemer/ui/player/LyricsScreen.kt` | 796 | `com.jtech.zemer.ui.player` | fun LyricsScreen, val context, val activity, val playerConnection, val player, val menuState, val database, val coroutineScope, val playbackState, val isPlaying, val repeatMode, val shuffleModeEnabled, val sliderStyle, val currentLyrics, val entryPoint, val lyricsHelper, val lyrics, var position, var duration, var sliderPosition, val playerBackground, val isSystemInDarkTheme, val useDarkTheme, var gradientColors, val gradientColorsCache |
| `app/src/main/kotlin/com/jtech/zemer/ui/player/MiniPlayer.kt` | 900 | `com.jtech.zemer.ui.player` | class MiniPlayerFocusTargets, val play, val account, val heart, val afterHeart, val down, fun Modifier, fun MiniPlayer, val useNewMiniPlayerDesign, fun NewMiniPlayer, val playerConnection, val database, val isPlaying, val playbackState, val error, val mediaMetadata, val canSkipNext, val canSkipPrevious, val layoutDirection, val coroutineScope, val swipeSensitivity, val swipeThumbnail, val configuration, val isTabletLandscape, val offsetXAnimatable |
| `app/src/main/kotlin/com/jtech/zemer/ui/player/PlaybackError.kt` | 45 | `com.jtech.zemer.ui.player` | fun PlaybackError |
| `app/src/main/kotlin/com/jtech/zemer/ui/player/Player.kt` | 1364 | `com.jtech.zemer.ui.player` | fun BottomSheetPlayer, val context, val clipboardManager, val menuState, val bottomSheetPageState, val playerConnection, val floatingMiniPlayerEnabled, val playerBackground, val playerButtonsStyle, val isSystemInDarkTheme, val darkTheme, val useDarkTheme, val onBackgroundColor, val useBlackBackground, val useDarkTheme, val playbackState, val isPlaying, val mediaMetadata, val currentSong, val automix, val repeatMode, val canSkipPrevious, val canSkipNext, val sliderStyle, var position |
| `app/src/main/kotlin/com/jtech/zemer/ui/player/Queue.kt` | 1131 | `com.jtech.zemer.ui.player` | fun Queue, val context, val haptic, val menuState, val bottomSheetPageState, val playerConnection, val isPlaying, val repeatMode, val currentWindowIndex, val mediaMetadata, val selectedSongs, val selectedItems, var selection, var locked, val snackbarHostState, var dismissJob, var showSleepTimerDialog, var sleepTimerValue, val sleepTimerEnabled, var sleepTimerTimeLeft, val buttonSize, val iconSize, val borderColor, val queueTitle, val queueWindows |
| `app/src/main/kotlin/com/jtech/zemer/ui/player/Thumbnail.kt` | 472 | `com.jtech.zemer.ui.player` | fun Thumbnail, val playerConnection, val context, val mediaMetadata, val error, val queueTitle, val swipeThumbnail, val hidePlayerThumbnail, val canSkipPrevious, val canSkipNext, val playerBackground, val textBackgroundColor, val thumbnailLazyGridState, val timeline, val currentIndex, val shuffleModeEnabled, val previousMediaMetadata, val previousIndex, val nextMediaMetadata, val nextIndex, val currentMediaItem, val mediaItems, val currentMediaIndex, val horizontalLazyGridItemWidthFactor, val thumbnailSnapLayoutInfoProvider |
| `app/src/main/kotlin/com/jtech/zemer/ui/screens/player/VideoPlayerScreen.kt` | 1114 | `com.jtech.zemer.ui.screens.player` | fun VideoPlayerScreen, val context, val activity, val clipboard, val connectivityManager, val database, val scope, val playerConnection, val lifecycleOwner, var videoItem, var playerInstance, var isLoading, var loadError, var showDownloadDialog, var showSpeedDialog, var showQualityDialog, var currentTitle, var reloadKey, var availableQualities, var selectedQualityId, var playbackInfo, var isInPipMode, var artistName, var positionMs, var durationMs |

## Manifest playback/download components

| Component | Exported | Foreground type / actions visible in manifest |
| --- | --- | --- |
| `.playback.MusicService` | `true` | `mediaPlayback`; actions `androidx.media3.session.MediaSessionService, androidx.media3.session.MediaLibraryService, android.media.browse.MediaBrowserService` |
| `.playback.ExoDownloadService` | `false` | `dataSync`; actions `androidx.media3.exoplayer.downloadService.action.RESTART` |
| `.playback.MediaStoreDownloadService` | `false` | `dataSync`; actions `android.accessibilityservice.AccessibilityService` |
| `androidx.media3.session.MediaButtonReceiver` | `true` | ``; actions `android.intent.action.MEDIA_BUTTON` |
